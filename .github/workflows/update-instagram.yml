name: Instagram feed retired
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'The main Instagram feed has been removed. No scheduled updates are needed.'
