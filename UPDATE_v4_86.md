# v4.86 HISTORY 이미지 폭 수정

모바일에서 하단 일러스트를 본문 폭 안으로 줄였습니다. PC에서도 화면 전체 너비가 아닌 사이드 메뉴를 제외한 본문 폭에 맞춰 표시합니다. 이미지 비율을 유지하며 최대 폭은 760px입니다. 가로 스크롤을 강제로 숨기는 방식은 사용하지 않았습니다.

320·360·390·700·701·768·900·1080·1200·1280·1920px 폭에서 가로 넘침이 없는 것을 확인했습니다. 세로 PC 900×1600 및 1080×1920 포함. 재현 빌드·JS 문법·11개 정적 본문·로컬 링크 검사 통과.

v4.85의 기사 154건 추가를 포함한 기존 데이터는 유지됩니다.

최신 변경을 Pull한 뒤 UPLOAD ZIP을 같은 경로에 복사하고 커밋·Push하세요. Actions 빌드가 끝나면 배포됩니다. 생성된 HTML 직접 편집 금지. 로컬 빌드는 node build.js로 실행합니다.

UPLOAD는 변경 소스만 포함하며 뉴스·방문자·사진·아카이브 데이터와 생성 HTML을 덮어쓰지 않습니다. FULL_BACKUP은 모든 소스와 빌드 산출물을 담은 보관용입니다.
